
# partnR 0.0.0.9000

## TODO

1. Start Workout Tab

   * Select Plan (Restore most recently Selected Plan
   * Select Workout (Autoselect from split day value)
   * "Begin workout" button
   
   
2. Workout Tab
   * [workout name]
   * [workout timer]
   
   * [exersize name]
   * [set number] / [num. sets]
   * [rest timer] / [set done button] (beeps when set should start again)
   * when excersize is done, button appears to begin next exersize

   
3. Make Plan Tab
   * Plan - plan name, split type (num. days)
     * Import option (csv with named columns)
       * workout_name, split_day, exersize_name, set_num, rest_time, weight, num_reps, note)
     * Manual input option
       * Workout - workout name, split day
         * Exersize - exersize name, num. sets, rest time
         * Set - weight, num. reps, note (e.g, drop set, burnout)
         